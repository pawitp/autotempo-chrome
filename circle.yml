machine:
  timezone:
    Asia/Bangkok
dependencies:
  post:
    - bower install
test:
  override:
    - grunt
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - cp temp/test/*.xml $CIRCLE_TEST_REPORTS/junit/
general:
  artifacts:
    - "package"

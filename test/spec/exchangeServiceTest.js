/*jshint expr: true*/
(function() {
  'use strict';

  beforeEach(module('exchangeService'));

  describe('exchangeService', function() {
    describe('getExchangeFolder', function() {
      it('should send correct request and read response', inject(function(exchangeService) {
        // TODO
        expect(exchangeService).to.exist;
      }));
    });
  });
})();
